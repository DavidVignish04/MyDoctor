<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Appointment | MedConnect</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f9ff;
            color: #333;
            line-height: 1.6;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        /* Header Styles */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            border-bottom: 1px solid #e0e7ff;
            margin-bottom: 30px;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
            color: #3a6bc7;
            font-size: 24px;
            font-weight: 700;
        }
        
        .logo i {
            color: #3a6bc7;
        }
        
        .nav-links {
            display: flex;
            gap: 30px;
        }
        
        .nav-links a {
            text-decoration: none;
            color: #555;
            font-weight: 500;
            transition: color 0.3s;
        }
        
        .nav-links a:hover, .nav-links a.active {
            color: #3a6bc7;
        }
        
        /* Booking Container */
        .booking-container {
            display: flex;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        .doctor-info {
            flex: 1;
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 150, 0.08);
            padding: 25px;
            height: fit-content;
        }
        
        .booking-form {
            flex: 2;
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 150, 0.08);
            padding: 30px;
        }
        
        .doctor-header {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid #eef3ff;
        }
        
        .doctor-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background-color: #e6eeff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            color: #3a6bc7;
        }
        
        .doctor-details h2 {
            color: #2a4a9c;
            margin-bottom: 5px;
        }
        
        .doctor-specialty {
            color: #3a6bc7;
            font-weight: 600;
            margin-bottom: 8px;
            display: block;
        }
        
        .doctor-rating {
            color: #ffb300;
            font-weight: 600;
        }
        
        .doctor-schedule h3 {
            color: #2a4a9c;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 1px solid #eef3ff;
        }
        
        .schedule-item {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px dashed #eef3ff;
        }
        
        .schedule-day {
            font-weight: 500;
            color: #555;
        }
        
        .schedule-time {
            color: #3a6bc7;
            font-weight: 500;
        }
        
        /* Form Styles */
        .form-header {
            color: #2a4a9c;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 1px solid #eef3ff;
        }
        
        .form-row {
            display: flex;
            gap: 20px;
            margin-bottom: 25px;
        }
        
        .form-group {
            flex: 1;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #555;
            font-weight: 500;
        }
        
        .form-control {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #d0ddf5;
            border-radius: 8px;
            font-size: 16px;
            transition: border 0.3s;
        }
        
        .form-control:focus {
            outline: none;
            border-color: #3a6bc7;
            box-shadow: 0 0 0 2px rgba(58, 107, 199, 0.1);
        }
        
        .time-slots {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            gap: 12px;
            margin-top: 8px;
        }
        
        .time-slot {
            padding: 12px;
            border: 1px solid #d0ddf5;
            border-radius: 8px;
            text-align: center;
            cursor: pointer;
            transition: all 0.2s;
        }
        
        .time-slot:hover {
            background-color: #f0f5ff;
            border-color: #3a6bc7;
        }
        
        .time-slot.selected {
            background-color: #3a6bc7;
            color: white;
            border-color: #3a6bc7;
        }
        
        .time-slot.unavailable {
            background-color: #f5f5f5;
            color: #aaa;
            cursor: not-allowed;
            border-color: #e0e0e0;
        }
        
        /* Confirmation Modal */
        .confirmation-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }
        
        .modal-content {
            background-color: white;
            border-radius: 15px;
            width: 90%;
            max-width: 500px;
            padding: 40px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            animation: modalFade 0.3s ease-out;
        }
        
        @keyframes modalFade {
            from { opacity: 0; transform: translateY(-30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .token-number {
            font-size: 48px;
            font-weight: 800;
            color: #3a6bc7;
            margin: 20px 0;
            letter-spacing: 3px;
        }
        
        .appointment-details {
            background-color: #f8fbff;
            padding: 20px;
            border-radius: 10px;
            margin: 20px 0;
            text-align: left;
        }
        
        .detail-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding-bottom: 10px;
            border-bottom: 1px dashed #e0e7ff;
        }
        
        .buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 30px;
        }
        
        .btn {
            padding: 12px 30px;
            border-radius: 8px;
            border: none;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .btn-primary {
            background-color: #3a6bc7;
            color: white;
        }
        
        .btn-primary:hover {
            background-color: #2a5ab7;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(58, 107, 199, 0.2);
        }
        
        .btn-secondary {
            background-color: #f0f5ff;
            color: #3a6bc7;
            border: 1px solid #d0ddf5;
        }
        
        .btn-secondary:hover {
            background-color: #e6eeff;
        }
        
        /* Alert Messages */
        .alert {
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            display: none;
        }
        
        .alert-success {
            background-color: #e8f7ef;
            color: #0a6b3c;
            border: 1px solid #b8e6cd;
        }
        
        .alert-error {
            background-color: #fde8e8;
            color: #c53030;
            border: 1px solid #fbb6b6;
        }
        
        /* Footer */
        .footer {
            text-align: center;
            padding: 20px 0;
            color: #666;
            font-size: 14px;
            border-top: 1px solid #e0e7ff;
            margin-top: 30px;
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .booking-container {
                flex-direction: column;
            }
            
            .form-row {
                flex-direction: column;
                gap: 15px;
            }
            
            .header {
                flex-direction: column;
                gap: 15px;
            }
            
            .nav-links {
                gap: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="logo">
                <i class="fas fa-stethoscope"></i>
                <span>MedConnect</span>
            </div>
            <div class="nav-links">
                <a href="/home" class="active">Home</a>
                <a href="/doctors">Doctors</a>
                <a href="/bookings">Appointments</a>
                <a href="#">Profile</a>
            </div>
        </div>

        
        <!-- Alert Messages -->
        <div id="alertSuccess" class="alert alert-success">
            <i class="fas fa-check-circle"></i> Appointment booked successfully! Please check the confirmation details.
        </div>
        
        <div id="alertError" class="alert alert-error">
            <i class="fas fa-exclamation-circle"></i> Selected time is not available. Please choose another time slot.
        </div>
        
        <!-- Booking Container -->
         <form action="/book-appointment" method="post">
            <div class="booking-container">
                <!-- Doctor Information -->
                <div class="doctor-info">
                    <div class="doctor-header">
                        <div class="doctor-avatar">
                            <i class="fas fa-user-md"></i>
                        </div>
                        <div class="doctor-details">
                            <h2 id="doctorName"> <%= doctor.name %></h2>
                            <span class="doctor-specialty" id="doctorSpecialty"> <%= doctor.specialty %></span>
                            <div class="doctor-rating" id="doctorRating">
                                <i class="fas fa-star"></i> <%= doctor.rating %>
                            </div>
                        </div>
                    </div>
                    
                    <div id="doctorSchedule">
                        <% for (let day in doctor.workingHours) { %>
                            <div class="schedule-item">
                                <span class="schedule-day"><%= day %></span>
                                <span class="schedule-time"><%= doctor.workingHours[day][0] %> - <%= doctor.workingHours[day][1] %></span>
                            </div>
                        <% } %>
                    </div>

                </div>
                
                <!-- Booking Form -->
                <div class="booking-form">
                    <h2 class="form-header">Book Your Appointment</h2>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label for="patientName">Full Name</label>
                            <input type="text" id="patientName" class="form-control" placeholder="Enter your full name" >
                        </div>
                        <div class="form-group">
                            <label for="patientPhone">Phone Number</label>
                            <input type="tel" inputmode="numeric" maxlength="10" pattern="[0-9]{10}" id="patientPhone" class="form-control" placeholder="Enter your phone number" >
                        </div>
                    </div>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label for="appointmentDate">Select Date</label>
                            <input type="date" id="appointmentDate" class="form-control">
                        </div>
                        <div class="form-group">
                            <label>Select Time Slot</label>
                            <div class="time-slots" id="timeSlots">
                                <!-- Time slots will be populated by JavaScript -->
                            </div>
                        </div>
                    </div>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label for="appointmentReason">Reason for Visit</label>
                            <textarea id="appointmentReason" class="form-control" rows="3" placeholder="Briefly describe the reason for your appointment">Annual skin checkup</textarea>
                        </div>
                    </div>
                    
                    <div class="buttons">
                        <button id="bookAppointmentBtn" type="button" class="btn btn-primary"  onclick="window.location.href='/doctors'; showSuccess() "
                            <i class="fas fa-calendar-check"></i> Book Appointment
                        </button>           
                        <button id="cancelBtn" class="btn btn-secondary">
                            Cancel
                        </button>
                    </div>
                </div>
            </div>
        </form>

        <!-- Footer -->
        <div class="footer">
            <p>© 2023 MedConnect. All rights reserved. | <a href="#" style="color:#3a6bc7;">Privacy Policy</a> | <a href="#" style="color:#3a6bc7;">Terms of Service</a></p>
        </div>
    </div>
    
    <!-- Confirmation Modal -->
    <div id="confirmationModal" class="confirmation-modal">
        <div class="modal-content">
            <i class="fas fa-check-circle" style="font-size: 60px; color: #4CAF50; margin-bottom: 20px;"></i>
            <h2>Appointment Confirmed!</h2>
            <p>Your appointment has been successfully booked.</p>
            
            <div class="token-number" id="tokenNumber">A-047</div>
            <p>Please show this token number at the reception.</p>
            
            <div class="appointment-details">
                <div class="detail-item">
                    <span>Doctor:</span>
                    <span id="confirmDoctor"></span>
                </div>
                <div class="detail-item">
                    <span>Date:</span>
                    <span id="confirmDate"></span>
                </div>
                <div class="detail-item">
                    <span>Time:</span>
                    <span id="confirmTime"></span>
                </div>
                <div class="detail-item">
                    <span>Patient:</span>
                    <span id="confirmPatient"></span>
                </div>
            </div>
            
            <div class="buttons">
                <button id="printBtn" class="btn btn-primary">
                    <i class="fas fa-print"></i> Print Details
                </button>
                <button id="closeModalBtn" class="btn btn-secondary">
                    Close
                </button>
            </div>
        </div>
    </div>

    <script>
        window.doctorData = <%- JSON.stringify(doctor) %>;
    </script>
    


    <script>
        // DOM Elements
        const doctor = window.doctorData;
        console.log("Loaded doctor:", doctor);

        const doctorNameElement = document.getElementById('doctorName');
        const doctorSpecialtyElement = document.getElementById('doctorSpecialty');
        const doctorRatingElement = document.getElementById('doctorRating');
        const doctorScheduleElement = document.getElementById('doctorSchedule');
        const timeSlotsElement = document.getElementById('timeSlots');
        const appointmentDateElement = document.getElementById('appointmentDate');
        const bookAppointmentBtn = document.getElementById('bookAppointmentBtn');
        const cancelBtn = document.getElementById('cancelBtn');
        const confirmationModal = document.getElementById('confirmationModal');
        const closeModalBtn = document.getElementById('closeModalBtn');
        const printBtn = document.getElementById('printBtn');
        const alertSuccess = document.getElementById('alertSuccess');
        const alertError = document.getElementById('alertError');
        
        // Confirmation modal elements
        const confirmDoctorElement = document.getElementById('confirmDoctor');
        const confirmDateElement = document.getElementById('confirmDate');
        const confirmTimeElement = document.getElementById('confirmTime');
        const confirmPatientElement = document.getElementById('confirmPatient');
        const tokenNumberElement = document.getElementById('tokenNumber');
        
        // State variables
        let selectedDoctor = "<%= doctor.name %>";;
        let selectedDate = "";
        let selectedTime = "";
        let availableTimeSlots = [];

        function selectTimeSlot(event, time) {
            document.querySelectorAll('.time-slot').forEach(s => s.classList.remove('selected'));
            event.target.classList.add('selected');
            selectedTime = time;
        }
        
        
        
        // Initialize the page
        function initPage() {
            // Set minimum date to today
            const today = new Date();
            const tomorrow = new Date(today);
            tomorrow.setDate(tomorrow.getDate() + 1);
            const minDate = tomorrow.toISOString().split('T')[0];
            appointmentDateElement.min = minDate;
            
            // Set default date to tomorrow
            appointmentDateElement.value = minDate;
            selectedDate = formatDate(tomorrow);
            
            generateTimeSlots();
            
            // Add event listeners
            bookAppointmentBtn.addEventListener('click', bookAppointment);
            cancelBtn.addEventListener('click', () => window.history.back());
            closeModalBtn.addEventListener('click', closeModal);
            printBtn.addEventListener('click', printDetails);
        }
        
        // Update doctor information
        function updateDoctorInfo(selectedDoctor) {
            const doctor = doctors[doctorName];
            doctorNameElement.textContent = doctorName;
            doctorSpecialtyElement.textContent = doctor.specialty;
            doctorRatingElement.innerHTML = `<i class="fas fa-star"></i> ${doctor.rating}`;
            selectedDoctor = doctorName;
            
            generateScheduleDisplay();
            generateTimeSlots();
        }
        
        // Generate schedule display
        function generateScheduleDisplay() {
            const doctor = doctors[selectedDoctor];
            let scheduleHTML = "";
            
            for (const [day, hours] of Object.entries(doctor.schedule)) {
                scheduleHTML += `
                    <div class="schedule-item">
                        <span class="schedule-day">${day}</span>
                        <span class="schedule-time">${hours}</span>
                    </div>
                `;
            }
            
            doctorScheduleElement.innerHTML = scheduleHTML;
        }
        
        // Generate time slots based on selected doctor and date
        function generateTimeSlots() {
            if (!doctor || !doctor.workingHours) {
                console.error("Doctor data missing:", doctor);
                return;
            }

            const dateValue = appointmentDateElement.value;

            if (!dateValue) {
                console.warn("No date selected yet.");
                return;
            }

            const date = new Date(dateValue + "T00:00:00");  // FIXED
            const dayName = date.toLocaleDateString("en-US", { weekday: "long" });

            const container = document.getElementById("timeSlots");
            container.innerHTML = "";

            const todaysHours = doctor.workingHours[dayName];

            if (!todaysHours) {
                container.innerHTML = `<p style="color:#c53030;">Doctor not available on ${dayName}</p>`;
                return;
            }

            const [start, end] = todaysHours;

            const startObj = parseTime(start);
            const endObj   = parseTime(end);

            let current = new Date();
            current.setHours(startObj.hours, startObj.minutes, 0, 0);

            let finish = new Date();
            finish.setHours(endObj.hours, endObj.minutes, 0, 0);

            while (current < finish) {
                let time = current.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });

                const div = document.createElement("div");
                div.className = "time-slot";
                div.textContent = time;
                div.onclick = () => selectSlot(div, time);
                container.appendChild(div);

                current.setMinutes(current.getMinutes() + 30);
            }
        }

        function selectSlot(div, time) {
            document.querySelectorAll(".time-slot").forEach(s => s.classList.remove("selected"));
            div.classList.add("selected");
            selectedTime = time;
        }
        function parseTime(t) {
            const [time, modifier] = t.split(" ");
            let [hours, minutes] = time.split(":").map(Number);

            if (modifier === "PM" && hours !== 12) hours += 12;
            if (modifier === "AM" && hours === 12) hours = 0;

            return { hours, minutes };
        }

        appointmentDateElement.addEventListener("change", () => {
            if (!appointmentDateElement.value) return;

            const date = new Date(appointmentDateElement.value + "T00:00:00");
            selectedDate = formatDate(date);
            generateTimeSlots();
        });

                // Book appointment
        function bookAppointment() {
            console.log("Book Appointment clicked");
            const patientName = document.getElementById('patientName').value;
            const patientPhone = document.getElementById('patientPhone').value;
            const appointmentReason = document.getElementById('appointmentReason').value;

            if (!patientName || !patientPhone || !selectedDate || !selectedTime) {
                showAlert("Fill all fields and select time", "error");
                return;
            }

            const data = {
                doctor: selectedDoctor,
                date: selectedDate,
                time: selectedTime,
                patientName,
                patientPhone,
                reason: appointmentReason
            };

            fetch("/book-appointment", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json"
                },
                body: JSON.stringify(data)
            })
            .then(res => res.json())
            .then(response => {
                if (response.success) {
                    confirmationModal.style.display = "flex";
                } else {
                    showAlert("Booking failed", "error");
                }
            })
            .catch(err => {
                console.error(err);
                showAlert("Server error", "error");
            });
        }

        
        // Show alert message
        function showAlert(message, type) {
            hideAlerts();
            
            if (type === "success") {
                alertSuccess.style.display = "block";
                alertSuccess.innerHTML = `<i class="fas fa-check-circle"></i> ${message}`;
            } else {
                alertError.style.display = "block";
                alertError.innerHTML = `<i class="fas fa-exclamation-circle"></i> ${message}`;
            }
            
            // Auto-hide after 5 seconds
            setTimeout(hideAlerts, 5000);
        }
        
        // Hide all alerts
        function hideAlerts() {
            alertSuccess.style.display = "none";
            alertError.style.display = "none";
        }
        
        // Close modal
        function closeModal() {
            confirmationModal.style.display = "none";
        }

        function showSuccess() {
            const toast = document.getElementById("successToast");
            toast.style.display = "block";

            setTimeout(() => {
                toast.style.display = "none";
            }, 3000); // hides after 3 seconds
        }

        
        // Print appointment details
        function printDetails() {
            const printContent = `
                <html>
                <head>
                    <title>MedConnect Appointment Confirmation</title>
                    <style>
                        body { font-family: Arial, sans-serif; padding: 20px; }
                        .header { text-align: center; margin-bottom: 30px; }
                        .token { font-size: 36px; font-weight: bold; color: #3a6bc7; margin: 20px 0; }
                        .details { border: 1px solid #ccc; padding: 20px; border-radius: 10px; }
                        .detail-row { display: flex; justify-content: space-between; margin-bottom: 10px; padding-bottom: 10px; border-bottom: 1px dashed #ddd; }
                        .footer { margin-top: 30px; text-align: center; color: #666; font-size: 12px; }
                    </style>
                </head>
                <body>
                    <div class="header">
                        <h1>MedConnect</h1>
                        <h2>Appointment Confirmation</h2>
                    </div>
                    <div style="text-align:center;">
                        <p>Your appointment has been confirmed. Please arrive 15 minutes early.</p>
                        <div class="token">${tokenNumberElement.textContent}</div>
                        <p>Please show this token at the reception.</p>
                    </div>
                    <div class="details">
                        <div class="detail-row">
                            <span>Doctor:</span>
                            <span>${confirmDoctorElement.textContent}</span>
                        </div>
                        <div class="detail-row">
                            <span>Date:</span>
                            <span>${confirmDateElement.textContent}</span>
                        </div>
                        <div class="detail-row">
                            <span>Time:</span>
                            <span>${confirmTimeElement.textContent}</span>
                        </div>
                        <div class="detail-row">
                            <span>Patient:</span>
                            <span>${confirmPatientElement.textContent}</span>
                        </div>
                    </div>
                    <div class="footer">
                        <p>© 2023 MedConnect. All rights reserved.</p>
                        <p>123 Medical Center Drive, Health City, HC 12345</p>
                    </div>
                </body>
                </html>
            `;
            
            const printWindow = window.open('', '_blank');
            printWindow.document.write(printContent);
            printWindow.document.close();
            printWindow.print();
        }
        
        // Format date to display
        function formatDate(date) {
            const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
            return date.toLocaleDateString('en-US', options);
        }
        
        // Initialize the page when DOM is loaded
        document.addEventListener('DOMContentLoaded', initPage);
    </script>
<div id="successToast" style="
    position: fixed;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    background: #2ecc71;
    color: white;
    padding: 14px 24px;
    border-radius: 8px;
    font-weight: 600;
    display: none;
    z-index: 9999;
">
    ✅ Booking successful
</div>

</body>
</html>